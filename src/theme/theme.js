import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  
    palette: {
     // type:'dark',
      primary: {
        main: '#8C3746',
        light: '#a35f6b',
        dark: '#622631',
      },
      secondary: {
        main: '#f44336',
      },
    },
  }
);

export default theme;

